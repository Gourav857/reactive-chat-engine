package com.enterprise.chat.engine.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisReactiveConfig {
    // Temporary placeholder to bypass tight coupling compiler errors
    // System will directly fall back to standard local memory stream
}